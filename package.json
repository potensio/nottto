{
  "name": "notto",
  "version": "2.1.0",
  "private": true,
  "description": "Notto - Screenshot annotation tool with Chrome extension and API",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "dev:extension": "pnpm --filter @notto/extension dev",
    "dev:api": "pnpm --filter @notto/api dev",
    "dev:web": "pnpm --filter @notto/web dev",
    "build:extension": "pnpm --filter @notto/extension build",
    "build:api": "pnpm --filter @notto/api build",
    "build:web": "pnpm --filter @notto/web build",
    "typecheck": "pnpm -r typecheck",
    "db:generate": "pnpm --filter @notto/api db:generate",
    "db:migrate": "pnpm --filter @notto/api db:migrate"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.0.0"
}
