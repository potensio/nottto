{
  "name": "nottto",
  "version": "2.1.0",
  "private": true,
  "description": "Nottto - Screenshot annotation tool with Chrome extension and API",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "dev:extension": "pnpm --filter @nottto/extension dev",
    "dev:api": "pnpm --filter @nottto/api dev",
    "build:extension": "pnpm --filter @nottto/extension build",
    "build:api": "pnpm --filter @nottto/api build",
    "typecheck": "pnpm -r typecheck",
    "db:generate": "pnpm --filter @nottto/api db:generate",
    "db:migrate": "pnpm --filter @nottto/api db:migrate"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.0.0"
}
